export default class MasFilters {
    static fromHash() {
        return new MasFilters();
    }

    /**
     * @param {MasFilters} other
     * @returns {boolean}
     */
    equals(other) {
        return true;
    }
}
